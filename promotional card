#Collection/page linked promotional card for client George(USA) of thk(UAE)

<style>
.customer-favorites-section {
  padding: 50px 20px;
  background-color: {{ section.settings.background_color }};
  font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, Oxygen, Ubuntu, Cantarell, sans-serif;
}
.cf-container { max-width: 1100px; margin: 0 auto; }
.cf-header { text-align: center; margin-bottom: 40px; }

.cf-subtitle {
  font-size: 16px; letter-spacing: 2px; text-transform: uppercase;
  color: {{ section.settings.subtitle_color }}; margin-bottom: 15px;
  font-weight: 400; position: relative; display: inline-block;
}
.cf-subtitle::before, .cf-subtitle::after {
  content: ''; position: absolute; top: 50%; width: 50px; height: 1px;
  background-color: {{ section.settings.subtitle_color }};
}
.cf-subtitle::before { right: calc(100% + 15px); }
.cf-subtitle::after { left: calc(100% + 15px); }

.cf-title { font-size: 36px; font-weight: 600; margin-bottom: 10px; line-height: 1.2; }
.cf-description {
  font-size: 18px; line-height: 1.6; max-width: 700px; margin: 0 auto; font-weight: 500;
}

.cf-products-grid {
  display: grid; grid-template-columns: repeat(3, 1fr); gap: 30px; margin-bottom: 40px;
}
.cf-product-card {
  background: {{ section.settings.card_background }};
  border-radius: 12px; overflow: hidden;
  box-shadow: 0 2px 15px rgba(0,0,0,0.08);
  transition: transform 0.3s ease, box-shadow 0.3s ease;
  border: 1px solid {{ section.settings.card_border_color }};
  display: flex; flex-direction: column;
}
.cf-product-card:hover { transform: translateY(-5px); box-shadow: 0 8px 25px rgba(0,0,0,0.12); }

.cf-product-image-wrapper { position: relative; padding: 15px; background-color: #fff; }
.cf-product-image-container {
  position: relative; padding-top: 75%; overflow: hidden; background-color: #f5f5f5;
  border: 1px solid {{ section.settings.image_border_color }};
  border-radius: {{ section.settings.image_border_radius }}px;
}
.cf-product-badge {
  position: absolute; top: 25px; left: 25px; padding: 6px 16px; border-radius: 20px;
  font-size: 11px; text-transform: uppercase; letter-spacing: 1px; font-weight: 600;
  z-index: 10; border: 1px solid;
}
.cf-product-image {
  position: absolute; top: 0; left: 0; width: 100%; height: 100%;
  object-fit: cover; transition: transform 0.5s ease;
}
.cf-product-card:hover .cf-product-image { transform: scale(1.05); }

.cf-product-content {
  padding: 20px;
  display: flex;
  flex-direction: column;
  flex: 1;
  text-align: center; /* ✅ Added to center title + description */
}

.cf-product-title {
  font-size: 20px;
  font-weight: 500;
  color: {{ section.settings.product_title_color }};
  margin-bottom: 5px;
}
.cf-product-desc {
  font-size: 16px;
  font-weight: 500;
  color: {{ section.settings.product_description_color }};
  margin-bottom: 15px;
}

.cf-add-to-cart {
  width: 100%; padding: 14px 20px;
  background-color: {{ section.settings.button_background }};
  color: {{ section.settings.button_text_color }};
  border: none; border-radius: 8px;
  font-size: 14px; font-weight: 500;
  cursor: pointer; transition: all 0.3s ease;
  text-transform: uppercase; letter-spacing: 1px;
  text-decoration: none; text-align: center;
}
.cf-add-to-cart:hover {
  background-color: {{ section.settings.button_hover_background }};
  transform: translateY(-2px);
}

@media (max-width: 768px) {
  .cf-products-grid { display: flex; overflow-x: scroll; gap: 20px; scroll-snap-type: x mandatory; }
  .cf-product-card { flex: 0 0 100%; scroll-snap-align: center; }
}
</style>


<div class="customer-favorites-section">
  <div class="cf-container">
    <div class="cf-header">
      {% if section.settings.subtitle != blank %}
        <div class="cf-subtitle">{{ section.settings.subtitle }}</div>
      {% endif %}
      {% if section.settings.title != blank %}
        <h2 class="cf-title" style="color: {{ section.settings.title_color }}">{{ section.settings.title }}</h2>
      {% endif %}
      {% if section.settings.description != blank %}
        <p class="cf-description" style="color: {{ section.settings.description_color }}">{{ section.settings.description }}</p>
      {% endif %}
    </div>

    <div class="cf-products-grid">
      {% for block in section.blocks %}
        {% assign product = block.settings.product %}
        {% assign collection = block.settings.collection %}
        {% if product != blank %}
          {% assign item_url = product.url %}
          {% assign item_title = product.title %}
          {% assign item_image = product.featured_image %}
          {% assign item_desc = product.description | strip_html | truncate: 100 %}
        {% elsif collection != blank %}
          {% assign item_url = collection.url %}
          {% assign item_title = collection.title %}
          {% assign item_image = collection.image %}
          {% assign item_desc = collection.description | strip_html | truncate: 100 %}
        {% endif %}

        <div class="cf-product-card" {{ block.shopify_attributes }}>
          <div class="cf-product-image-wrapper">
            {% if block.settings.badge_text != blank %}
              <div class="cf-product-badge"
                style="background-color: {{ block.settings.badge_background }};
                       color: {{ block.settings.badge_text_color }};
                       border-color: {{ block.settings.badge_border_color }};">
                {{ block.settings.badge_text }}
              </div>
            {% endif %}

            <div class="cf-product-image-container">
              {% if block.settings.custom_image != blank %}
                <a href="{{ item_url }}">
                  <img class="cf-product-image" src="{{ block.settings.custom_image | img_url: '600x' }}" alt="{{ item_title }}" loading="lazy">
                </a>
              {% elsif item_image %}
                <a href="{{ item_url }}">
                  <img class="cf-product-image" src="{{ item_image | img_url: '600x' }}" alt="{{ item_title }}" loading="lazy">
                </a>
              {% else %}
                <img class="cf-product-image" src="{{ 'product-placeholder.svg' | asset_url }}" alt="Placeholder">
              {% endif %}
            </div>
          </div>

          <div class="cf-product-content">
            <h3 class="cf-product-title">
              <a href="{{ item_url }}" style="color: inherit; text-decoration: none;">{{ item_title }}</a>
            </h3>

            <p class="cf-product-desc">
              {% if block.settings.custom_description != blank %}
                {{ block.settings.custom_description }}
              {% else %}
                {{ item_desc }}
              {% endif %}
            </p>

            <a href="{{ item_url }}" class="cf-add-to-cart">{{ block.settings.button_text }}</a>
          </div>
        </div>
      {% endfor %}
    </div>
  </div>
</div>

{% schema %}
{
  "name": "Promotional Card",
  "settings": [
    { "type": "color", "id": "background_color", "label": "Background Color", "default": "#ffffff" },
    { "type": "text", "id": "subtitle", "label": "Subtitle", "default": "Featured Picks" },
    { "type": "color", "id": "subtitle_color", "label": "Subtitle Color", "default": "#999999" },
    { "type": "text", "id": "title", "label": "Title", "default": "Our Favorites" },
    { "type": "color", "id": "title_color", "label": "Title Color", "default": "#111111" },
    { "type": "textarea", "id": "description", "label": "Description", "default": "Explore our most-loved products and collections curated for you." },
    { "type": "color", "id": "description_color", "label": "Description Color", "default": "#666666" },
    { "type": "color", "id": "card_background", "label": "Card Background", "default": "#ffffff" },
    { "type": "color", "id": "card_border_color", "label": "Card Border Color", "default": "#e2e2e2" },
    { "type": "color", "id": "image_border_color", "label": "Image Border Color", "default": "#e2e2e2" },
    { "type": "range", "id": "image_border_radius", "label": "Image Border Radius", "min": 0, "max": 30, "step": 1, "default": 12, "unit": "px" },
    { "type": "color", "id": "product_title_color", "label": "Title Color", "default": "#111111" },
    { "type": "color", "id": "product_description_color", "label": "Description Color", "default": "#666666" },
    { "type": "color", "id": "button_background", "label": "Button Background", "default": "#111111" },
    { "type": "color", "id": "button_text_color", "label": "Button Text Color", "default": "#ffffff" },
    { "type": "color", "id": "button_hover_background", "label": "Button Hover Background", "default": "#333333" }
  ],
  "blocks": [
    {
      "type": "item",
      "name": "Product / Collection Card",
      "limit": 12,
      "settings": [
        { "type": "product", "id": "product", "label": "Select Product (optional)" },
        { "type": "collection", "id": "collection", "label": "Select Collection (optional)" },
        { "type": "image_picker", "id": "custom_image", "label": "Custom Image" },
        { "type": "text", "id": "badge_text", "label": "Badge Text" },
        { "type": "color", "id": "badge_background", "label": "Badge Background", "default": "#ff0000" },
        { "type": "color", "id": "badge_text_color", "label": "Badge Text Color", "default": "#ffffff" },
        { "type": "color", "id": "badge_border_color", "label": "Badge Border Color", "default": "#ff0000" },
        { "type": "text", "id": "custom_description", "label": "Custom Description" },
        { "type": "text", "id": "button_text", "label": "Button Text", "default": "View Details" }
      ]
    }
  ],
  "presets": [
    { "name": "Promotional Card", "category": "Products" }
  ]
}
{% endschema %}
