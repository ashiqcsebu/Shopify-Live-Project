{% schema %}
{
  "name": "Testimonial Carousel",
  "tag": "section",
  "settings": [],
  "blocks": [
    {
      "type": "testimonial",
      "name": "Testimonial",
      "settings": [
        { "type": "text", "id": "name", "label": "Customer Name" },
        { "type": "text", "id": "title", "label": "Review Title" },
        { "type": "textarea", "id": "review", "label": "Review Text" },
        { "type": "product", "id": "product", "label": "Product" }
      ]
    }
  ],
  "max_blocks": 12,
  "presets": [
    { "name": "Testimonial Carousel", "category": "Custom" }
  ]
}
{% endschema %}

<!-- Slick Carousel CSS -->
<link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/slick-carousel/1.8.1/slick.min.css" />
<link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/slick-carousel/1.8.1/slick-theme.min.css" />

<style>
/* Container & Header */
.testimonial-carousel-wrapper {
  max-width: 1500px;
  margin: auto;
  padding: 36px 1.5rem;
  position: relative;
}

.testimonial-header {
  display: flex;
  justify-content: space-between;
  align-items: center;
  margin-bottom: 36px;
}

.section-title {
  font-size: 3rem;
  font-weight: 800;
  line-height: 1.2;
  color: #47A876;
}

/* Carousel Arrows */
.carousel-arrows {
  display: flex;
  gap: 20px;
  font-size: 32px;
  cursor: pointer;
  z-index: 5;
}

.carousel-arrows span {
  display: flex;
  align-items: center;
  justify-content: center;
  width: 48px;
  height: 48px;
  border-radius: 50%;
  background: #fff;
  font-size: 28px;
  transition: transform 0.3s ease;
}

.carousel-arrows span:hover {
  transform: scale(1.1);
}

/* Carousel & Cards */
.testimonial-carousel {
  margin-top: 20px;
  display: flex;
  align-items: stretch; /* ensures equal height */
}

.testimonial-card {
  border-radius: 20px;
  padding: 36px 28px;
  display: flex;
  flex-direction: column;
  justify-content: flex-start;
  background: #fff;
  border: 1px solid #ddd; /* 5px border */
  margin: 20px 12px;
  transition: transform 0.3s ease;
  height: 100%; /* equal height within Slick slide */
  box-sizing: border-box;
}

.testimonial-card:hover {
  transform: translateY(-8px);
  box-shadow: none; /* shadow removed */
}

.testimonial-card .stars {
  color: #ffc107;
  font-size: 2.2rem;
  margin-bottom: 12px;
}

.testimonial-card h4 {
  font-weight: 700;
  margin: 16px 0 12px;
  font-size: 1.6rem;
}

.testimonial-card p {
  font-size: 1.25rem;
  color: #555;
  margin-bottom: 16px;
  line-height: 1.6;
}

.testimonial-card strong {
  font-size: 1.2rem;
  display: block;
  margin-bottom: 16px;
}

/* Product Info */
.testimonial-product {
  display: flex;
  align-items: center;
  padding: 14px 0;
  border-top: 1px solid #eee;
  gap: 16px;
}

.testimonial-product img {
  width: 80px;
  height: 90px;
  object-fit: cover;
  border-radius: 10px;
}

.testimonial-product div {
  font-size: 1.1rem;
  font-weight: 600;
  line-height: 1.3;
}

.testimonial-product s {
  color: #777;
  margin-left: 6px;
}

/* Slick Slide Padding */
.slick-slide {
  padding: 0 12px;
  display: flex !important; /* make each slide a flex container */
}

/* Responsive */
@media (max-width: 1200px) {
  .testimonial-card { height: auto; }
}

@media (max-width: 992px) {
  .testimonial-card { height: auto; }
}

@media (max-width: 768px) {
  .testimonial-card { height: auto; }
  .testimonial-header { flex-direction: column; align-items: flex-start; gap: 20px; }
  .carousel-arrows { margin-top: 10px; }
}
</style>

<div class="testimonial-carousel-wrapper">
  <div class="testimonial-header">
    <h2 class="section-title">Happy <strong>Customers</strong></h2>
    <div class="carousel-arrows">
      <span class="prev-arrow">‹</span>
      <span class="next-arrow">›</span>
    </div>
  </div>

  <div class="testimonial-carousel">
    {% for block in section.blocks %}
      {% assign product = all_products[block.settings.product] %}
      <div class="testimonial-card">
        <div class="stars">★★★★★</div>
        <h4>{{ block.settings.title }}</h4>
        <p>{{ block.settings.review }}</p>
        <strong>{{ block.settings.name }}</strong>

        {% if product %}
          <div class="testimonial-product">
            <img src="{{ product.featured_image | img_url: 'small' }}" alt="{{ product.title }}">
            <div>
              <div>{{ product.title }}</div>
              <div>
                {% if product.compare_at_price > product.price %}
                  <span style="color:#e60023;">{{ product.price | money }}</span>
                  <s>{{ product.compare_at_price | money }}</s>
                {% else %}
                  {{ product.price | money }}
                {% endif %}
              </div>
            </div>
          </div>
        {% endif %}
      </div>
    {% endfor %}
  </div>
</div>

<!-- jQuery & Slick JS -->
<script src="https://cdnjs.cloudflare.com/ajax/libs/jquery/3.6.0/jquery.min.js"></script>
<script src="https://cdnjs.cloudflare.com/ajax/libs/slick-carousel/1.8.1/slick.min.js"></script>

<script>
document.addEventListener("DOMContentLoaded", function () {
  if (typeof jQuery !== 'undefined' && typeof jQuery.fn.slick !== 'undefined') {
    const $carousel = jQuery('.testimonial-carousel');

    $carousel.slick({
      slidesToShow: 3,
      slidesToScroll: 1,
      arrows: false,
      dots: false,
      infinite: true,
      adaptiveHeight: false, // keep all cards same height
      responsive: [
        { breakpoint: 1500, settings: { slidesToShow: 3 }},
        { breakpoint: 1200, settings: { slidesToShow: 3 }},
        { breakpoint: 992,  settings: { slidesToShow: 2 }},
        { breakpoint: 768,  settings: { slidesToShow: 2 }},
        { breakpoint: 576,  settings: { slidesToShow: 1 }}
      ]
    });

    jQuery('.prev-arrow').click(function(){ $carousel.slick('slickPrev'); });
    jQuery('.next-arrow').click(function(){ $carousel.slick('slickNext'); });
  } else {
    console.error("jQuery or Slick not loaded properly");
  }
});
</script>
